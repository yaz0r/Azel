#pragma once

p_workArea overlayStart_BTL_A3(p_workArea);

// TODO: move all that stuff
void battle_A3_initMusic(p_workArea pThis);
void initMemoryForBattle(p_workArea pThis, const char** assetList);
