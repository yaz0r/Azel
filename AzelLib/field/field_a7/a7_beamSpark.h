#pragma once

#include "task.h"
#include "common.h"

// 06055aea — build the 0x30-byte beam-spark arg and tail-call the spawn wrapper
void a7BeamEmitter_spawnSpark_06055aea(p_workArea parent, sVec3_FP* pPos);
